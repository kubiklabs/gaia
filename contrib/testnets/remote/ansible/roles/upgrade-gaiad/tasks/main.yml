---

- name: Copy binary
  copy:
    src: "{{BINARY}}"
    dest: /usr/bin/junod
    mode: 0755
  notify: restart junod

- name: Copy new genesis.json file, if available
  when: "GENESISFILE is defined and GENESISFILE != ''" 
  copy:
    src: "{{GENESISFILE}}"
    dest: /home/junod/.juno/config/genesis.json
  notify: restart junod

- name: Download genesis.json URL, if available
  when: "GENESISURL is defined and GENESISURL != ''"
  get_url:
    url: "{{GENESISURL}}"
    dest: /home/junod/.juno/config/genesis.json
    force: yes
  notify: restart junod

- name: Reset network
  when: UNSAFE_RESET_ALL | default(false) | bool
  command: "sudo -u junod junod unsafe-reset-all"
  notify: restart junod

